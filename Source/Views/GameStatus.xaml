<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"                    
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    mc:Ignorable="d">

    <Style x:Key="ButtonGameStatusAction" BasedOn="{StaticResource {x:Type ButtonEx}}" TargetType="ButtonEx">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="50" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Background="{TemplateBinding Background}">
                        <Border CornerRadius="5" Opacity="0.8" Background="{DynamicResource OverlayMenu}"
                                x:Name="MainBackground"/>
                        <Border x:Name="MainBorder" Margin="-1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                Tag="False" BorderBrush="{DynamicResource ButtonBrushSetting}"
                                BorderThickness="3" CornerRadius="5" Visibility="Collapsed" />
                        <TextBlock x:Name="Text" Style="{DynamicResource TextBlockBaseStyle}"
                                   Foreground="{TemplateBinding Foreground}" FontWeight="SemiBold"
                                   HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                   VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                   Margin="0,0,0,5">
							<ContentPresenter />
                        </TextBlock>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>

                        <Trigger Property="IsFocused" Value="True">

                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Background" Value="{DynamicResource TopBarBackgroundBrush}" TargetName="MainBackground" />
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Background" Value="{DynamicResource TopBarBackgroundBrush}" TargetName="MainBackground" />

                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="Transparent" />
                        </Trigger>
                        <DataTrigger Binding="{Binding ElementName=SuccessButton, Path=IsChecked}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=SuccessButton, Path=IsChecked}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type GameStatus}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GameStatus}">
                    <Grid Background="{DynamicResource MainBackgourndBrush}"
                          x:Name="PART_ViewHost"
                          d:DesignWidth="{StaticResource DesignWidth}"
                          d:DesignHeight="{StaticResource DesignHeight}"
                          d:DataContext="{x:Static DesignMainViewModel.DesignSelectedGameIntance}"
                          KeyboardNavigation.DirectionalNavigation="Contained"
                          KeyboardNavigation.TabNavigation="Cycle"
                          KeyboardNavigation.ControlTabNavigation="Cycle">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="3*" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Image Source="{Binding Game.BackgroundImage, Converter={StaticResource ImageStringToImageConverterCached}}"
                               RenderOptions.BitmapScalingMode="Fant" Panel.ZIndex="-45"
                               Grid.RowSpan="5" Grid.ColumnSpan="3" RenderTransformOrigin="0.5,0.5">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Setter Property="Stretch" Value="UniformToFill"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Source}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>

                        <Grid Grid.ColumnSpan="3" Margin="-10,0,-10,-10" Height="220" Grid.RowSpan="1" VerticalAlignment="Bottom" Grid.Row="5" Opacity="0.7">
                            <Border BorderBrush="{DynamicResource TextBrush}" BorderThickness="3" />
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Background" Value="{DynamicResource ControlBackgroundDarkBrush}"/>
                                </Style>
                            </Grid.Style>
                        </Grid>

                        <Grid Grid.Row="5" Grid.Column="0" HorizontalAlignment="Center" Width="1155" Height="219.927" Margin="395,20,370,0" VerticalAlignment="Bottom" Grid.ColumnSpan="3">


                            
                            <StackPanel Orientation="Horizontal" Margin="0,-25,0,0"
                                    HorizontalAlignment="Center"
                                    Grid.Row="3" Grid.Column="1">
                                <TextBlock x:Name="PART_TextStatus" TextWrapping="Wrap"
                                   Style="{StaticResource TextBlockBaseStyle}" 
                                   HorizontalAlignment="Center" FontSize="45" Height="139.854" TextAlignment="Center"                                
                                   RenderTransformOrigin="0.5,0.5" Width="1046.912" Margin="20,60,20,0" VerticalAlignment="Top">

                                    <TextBlock.Effect>
                                        <DropShadowEffect ShadowDepth="2" Color="{DynamicResource ControlBackgroundColorDark}"
                                                  RenderingBias="Quality" BlurRadius="2" Opacity="1"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="300,120,300,0"
                                    HorizontalAlignment="Center"
                                    Grid.Row="0" Grid.Column="0" Width="555">


                                <ButtonEx 
										Style="{StaticResource ButtonGameStatusAction}" Height="49.927" 
										Command="{Binding DataContext.CloseGameStatusCommand, RelativeSource={RelativeSource AncestorType=Main}}"
										Content="{DynamicResource LOCOpenPlaynite}" Width="254.991" Margin="10,5,10,0" VerticalAlignment="Top"/>

                                <ButtonEx 
                Visibility="{PluginSettings Plugin=BackToGame, Path=IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}"
                Style="{StaticResource ButtonGameStatusAction}" Width="254.991" Margin="10,5,0,0"
                Content="{DynamicResource LOCBackToGame}"
                Command="{PluginSettings Plugin=BackToGame, Path=ActivateGame}" Height="49.927" VerticalAlignment="Top"/>

                            </StackPanel>
                            
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>