<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    mc:Ignorable="d">

	<Style x:Key="ListGameItemTemplate" TargetType="{x:Type GameListItem}">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type GameListItem}">
					<Grid>
						<!-- Utilisation de l'arrière-plan des jeux au lieu de la couverture -->
						<Image x:Name="PART_ImageCover" Visibility="Collapsed"
							   RenderOptions.BitmapScalingMode="Fant" StretchDirection="Both"
							   VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
							   Stretch="{Settings CoverArtStretch}" />
                        <Border x:Name="MainBorder" Opacity="0" BorderThickness="5" Margin="-2" CornerRadius="5" Background="{DynamicResource SelectionBrush}" Tag="False" 
                                Visibility="Collapsed" >
                        </Border>

                        <Border Background="{DynamicResource UninstalledColorBrush}" x:Name="BorderDarken" Visibility="Collapsed" />
						<TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis"
								   Visibility="{Settings Fullscreen.ShowGameTitles}"
								   Style="{DynamicResource TextBlockBaseStyle}"
								   FontSize="{DynamicResource FontSizeSmall}"
								   HorizontalAlignment="Stretch" VerticalAlignment="Bottom"
								   TextAlignment="Center" Padding="5"
								   Background="{DynamicResource ControlBackgroundDarkBrush}"/>
                        <TextBlock x:Name="InstallFont" Visibility="Collapsed" Text="&#xeee5;" 
                                   FontFamily="{StaticResource FontIcoFont}" Opacity="0"
                                   FontSize="65" Foreground="#eaeaea"
                                   VerticalAlignment="Bottom" HorizontalAlignment="Left"
                                   TextAlignment="Center" Margin="10,0,0,-5"  />

                    </Grid>
					<ControlTemplate.Triggers>
    
                        <MultiDataTrigger>
							<MultiDataTrigger.Conditions>
								<Condition Binding="{Binding IsInstalled}" Value="False" />
								<Condition Binding="{Settings Fullscreen.DarkenUninstalledGamesGrid}" Value="True" />
							</MultiDataTrigger.Conditions>
							<Setter Property="Visibility" Value="Visible" TargetName="BorderDarken" />
                            <Setter Property="Opacity" Value="0.6" TargetName="BorderDarken" />
						</MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsInstalled}" Value="False" />
                                <Condition Binding="{Settings Fullscreen.DarkenUninstalledGamesGrid}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" TargetName="InstallFont" />
                            <Setter Property="Opacity" Value="1" TargetName="InstallFont" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=ChangeViewButton, Path=IsChecked}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"  TargetName="PART_ImageCover" />
                            

                        </MultiDataTrigger>

                    </ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
    
    

</ResourceDictionary>