<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="ToggleButton">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Padding" Value="12,5,12,5" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="BorderBrush" Value="{DynamicResource SelectionLightBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid Background="{TemplateBinding Background}">
                        <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                   Foreground="{TemplateBinding Foreground}"
                                   HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                   VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                   Margin="{TemplateBinding Padding}">                            
                                <ContentPresenter />
                        </TextBlock>

                        <Border BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                x:Name="BorderStandard" />

                        <Border x:Name="BorderFocus" Margin="-3"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                BorderBrush="{DynamicResource SelectionBrush}"
                                BorderThickness="3" Visibility="Hidden" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ControlBackgroundDarkBrush}" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BorderFocus" />
                            <Setter Property="Visibility" Value="Hidden" TargetName="BorderStandard" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ControlBackgroundDarkBrush}" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BorderFocus" />
                            <Setter Property="Visibility" Value="Hidden" TargetName="BorderStandard" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource SelectionBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SuccessViewButton" TargetType="ToggleButton">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Foreground" Value="{DynamicResource TextWhiteBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="50" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="IsChecked" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid x:Name="MainGrid" Background="Transparent" >
                        <Border CornerRadius="5" Opacity="0.8" Background="{DynamicResource OverlayMenu}" x:Name="MainBackground"/>
                        <Border x:Name="MainBorder" Margin="-1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                Tag="False" BorderBrush="{DynamicResource ButtonBrushSetting}"
                                BorderThickness="3" CornerRadius="5" Visibility="Collapsed" />

                        <Border x:Name="BorderFocus" Margin="-1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                BorderBrush="{DynamicResource ButtonBrushSetting}" CornerRadius="5"
                                BorderThickness="3" Visibility="Collapsed"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <StackPanel x:Name="OFF" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,0,0,0" >
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <TextBlock Text="{DynamicResource Trophy}" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2" TextAlignment="Center" MaxWidth="210" 
                                                   Foreground="{DynamicResource TextBrush}" TextTrimming="WordEllipsis" Height="48" FontSize="28" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock Text="{DynamicResource Trophy}" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2" TextAlignment="Center" MaxWidth="210" 
                                                   Foreground="{DynamicResource TextBrush}" TextTrimming="WordEllipsis"  Height="47" FontSize="28" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock  Text="{DynamicResource Trophy}" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2" TextAlignment="Center" MaxWidth="210" 
                                                   Foreground="{DynamicResource TextBrush}" TextTrimming="WordEllipsis"  Height="46" FontSize="28" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel x:Name="ON" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,0,0,0" >
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <TextBlock Text="{DynamicResource HideTrophy}" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2" TextAlignment="Center"
                                                   Foreground="{DynamicResource TextBrush}" TextTrimming="WordEllipsis" MaxWidth="210" Height="45.72" FontSize="28" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Style.Triggers>

                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                        </StackPanel>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Background" Value="{DynamicResource TopBarBackgroundBrush}" TargetName="MainBackground" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BorderFocus" />
                            <Setter Property="Opacity" Value="1" TargetName="OFF" />
                            <Setter Property="Opacity" Value="1" TargetName="ON" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Background" Value="Transparent" TargetName="MainBackground" />
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="OFF" />
                            <Setter Property="Opacity" Value="1" TargetName="ON" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Visibility" TargetName="ON" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Visibility" TargetName="OFF" Value="Collapsed"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding ElementName=PART_ElemGameDetails, Path=Visibility}" Value="Collapsed">
                            <Setter Property="IsChecked" Value="True" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DLCViewButton" TargetType="ToggleButton">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Foreground" Value="{DynamicResource TextWhiteBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="50" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="IsChecked" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid x:Name="MainGrid" Background="Transparent" >
                        <Border CornerRadius="5" Opacity="0.8" Background="{DynamicResource OverlayMenu}" x:Name="MainBackground"/>
                        <Border x:Name="MainBorder" Margin="-1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                Tag="False" BorderBrush="{DynamicResource ButtonBrushSetting}"
                                BorderThickness="3" CornerRadius="5" Visibility="Collapsed" />

                        <Border x:Name="BorderFocus" Margin="-1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                BorderBrush="{DynamicResource ButtonBrushSetting}" CornerRadius="5"
                                BorderThickness="3" Visibility="Collapsed"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <StackPanel x:Name="OFF" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,0,0,0" >
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <TextBlock Text="DLC" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2"
                                                   Foreground="{DynamicResource TextBrush}" Height="48" FontSize="28" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock Text="DLC" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2"
                                                   Foreground="{DynamicResource TextBrush}" Height="47" FontSize="28" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock  Text="DLC" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2"
                                                   Foreground="{DynamicResource TextBrush}" Height="46" FontSize="28" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel x:Name="ON" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,0,0,0" >
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <TextBlock Text="HIDE DLC" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2"
                                                   Foreground="{DynamicResource TextBrush}" Height="45.72" FontSize="28" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Style.Triggers>

                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                        </StackPanel>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Background" Value="{DynamicResource TopBarBackgroundBrush}" TargetName="MainBackground" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BorderFocus" />
                            <Setter Property="Opacity" Value="1" TargetName="OFF" />
                            <Setter Property="Opacity" Value="1" TargetName="ON" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Background" Value="Transparent" TargetName="MainBackground" />
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="OFF" />
                            <Setter Property="Opacity" Value="1" TargetName="ON" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Visibility" TargetName="ON" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Visibility" TargetName="OFF" Value="Collapsed"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding ElementName=PART_ElemGameDetails, Path=Visibility}" Value="Collapsed">
                            <Setter Property="IsChecked" Value="True" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DetailsViewButton" TargetType="ToggleButton">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Foreground" Value="{DynamicResource TextWhiteBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="50" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="IsChecked" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid x:Name="MainGrid" Background="Transparent" >
                        <Border CornerRadius="5" Opacity="0.8" Background="{DynamicResource OverlayMenu}" x:Name="MainBackground"/>
                        <Border x:Name="MainBorder" Margin="-1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                Tag="False" BorderBrush="{DynamicResource ButtonBrushSetting}"
                                BorderThickness="3" CornerRadius="5" Visibility="Collapsed" />

                        <Border x:Name="BorderFocus" Margin="-1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                BorderBrush="{DynamicResource ButtonBrushSetting}" CornerRadius="5"
                                BorderThickness="3" Visibility="Collapsed"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <StackPanel x:Name="OFF" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,0,0,0" >
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <TextBlock Text="{DynamicResource ViewDetails}" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2" TextAlignment="Center"
                                                   Foreground="{DynamicResource TextBrush}" TextTrimming="WordEllipsis" MaxWidth="210" Height="48" FontSize="28" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock Text="{DynamicResource ViewDetails}" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2" TextAlignment="Center"
                                                   Foreground="{DynamicResource TextBrush}" TextTrimming="WordEllipsis" MaxWidth="210" Height="47" FontSize="28" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock  Text="{DynamicResource ViewDetails}" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2" TextAlignment="Center"
                                                   Foreground="{DynamicResource TextBrush}" TextTrimming="WordEllipsis" MaxWidth="210" Height="46" FontSize="28" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel x:Name="ON" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,0,0,0" >
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <TextBlock Text="{DynamicResource HideDetails}" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2" TextAlignment="Center"
                                                   Foreground="{DynamicResource TextBrush}" TextTrimming="WordEllipsis" MaxWidth="210" Height="45.72" FontSize="28" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Style.Triggers>

                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                        </StackPanel>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Background" Value="{DynamicResource TopBarBackgroundBrush}" TargetName="MainBackground" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BorderFocus" />
                            <Setter Property="Opacity" Value="1" TargetName="OFF" />
                            <Setter Property="Opacity" Value="1" TargetName="ON" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Background" Value="Transparent" TargetName="MainBackground" />
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="OFF" />
                            <Setter Property="Opacity" Value="1" TargetName="ON" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Visibility" TargetName="ON" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Visibility" TargetName="OFF" Value="Collapsed"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding ElementName=PART_ElemGameDetails, Path=Visibility}" Value="Collapsed">
                            <Setter Property="IsChecked" Value="True" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="SuccessChangeButton" TargetType="ToggleButton">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Foreground" Value="Transparent" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="60" />
        <Setter Property="Width" Value="60" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid x:Name="MainGrid" Background="Transparent" >
                        <Border CornerRadius="5" Opacity="0.8" Background="{DynamicResource OverlayMenu}" x:Name="MainBackground"/>
                        <Border x:Name="MainBorder" Margin="-1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                Tag="False" BorderBrush="{DynamicResource ButtonBrushSetting}"
                                BorderThickness="3" CornerRadius="5" Visibility="Collapsed" />

                        <Border x:Name="BorderFocus" Margin="-1"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                BorderBrush="{DynamicResource ButtonBrushSetting}" CornerRadius="5"
                                BorderThickness="3" Visibility="Collapsed"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <StackPanel x:Name="OFF" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,0,0,0" >
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <TextBlock Text="Locked Trophies" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,2,2,14"
                                                   Foreground="{DynamicResource TextBrush}" Height="48" FontSize="39" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock Text="Locked Trophies" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,2,2,14"
                                                   Foreground="{DynamicResource TextBrush}" Height="48" FontSize="39" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock Text="Locked Trophies" Style="{DynamicResource TextBlockBaseStyle}"                                                  
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,2,2,14"
                                                   Foreground="{DynamicResource TextBrush}" Height="48" FontSize="39" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel x:Name="ON" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,0,0,0" >
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <TextBlock Text="Unlocked Trophies" Style="{DynamicResource TextBlockBaseStyle}"                                                   
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,2,2,14"
                                                   Foreground="{DynamicResource TextBrush}" Height="48" FontSize="39" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Style.Triggers>

                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                        </StackPanel>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Background" Value="{DynamicResource TopBarBackgroundBrush}" TargetName="MainBackground" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BorderFocus" />
                            <Setter Property="Opacity" Value="1" TargetName="OFF" />
                            <Setter Property="Opacity" Value="1" TargetName="ON" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Background" Value="Transparent" TargetName="MainBackground" />
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="OFF" />
                            <Setter Property="Opacity" Value="1" TargetName="ON" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Visibility" TargetName="ON" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Visibility" TargetName="OFF" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Visibility" Value="Collapsed">
                            <Setter Property="Focusable" Value="False"/>
                        </Trigger>
                        <Trigger Property="Visibility" Value="Hidden">
                            <Setter Property="Focusable" Value="False"/>
                        </Trigger>
                        <Trigger Property="Visibility" Value="Visible">
                            <Setter Property="Focusable" Value="True"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding ElementName=SuccessButton, Path=IsChecked}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=PART_ElemGameDetails, Path=Visibility}" Value="Collapsed">
                            <Setter Property="IsChecked" Value="False" />
                        </DataTrigger>


                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="FilterViewButton" TargetType="ToggleButton">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Foreground" Value="Transparent" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="60" />
        <Setter Property="Width" Value="60" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid x:Name="MainGrid" Background="Transparent" >
                        <Border CornerRadius="5" Opacity="0.4" Background="Transparent" x:Name="MainBackground"/>
                        <Border x:Name="MainBorder" Opacity="0" Margin="-6"
										SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
											Tag="False"
											BorderThickness="3" CornerRadius="5" Visibility="Collapsed" />
                        <Border x:Name="BorderFocus" Margin="-3"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                BorderBrush="{DynamicResource ButtonBrushSetting}" CornerRadius="5"
                                BorderThickness="3" Visibility="Collapsed"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <StackPanel x:Name="OFF" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,0,0,0" >
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <Image RenderOptions.BitmapScalingMode="Fant" Height="130" Margin="2,2,5,25"
									   Source="{DynamicResource SourceLogo2}"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <Image RenderOptions.BitmapScalingMode="Fant" Height="130" Margin="2,2,5,25"
									   Source="{DynamicResource SourceLogo2}"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <Image RenderOptions.BitmapScalingMode="Fant" Height="130" Margin="2,2,5,25"
									   Source="{DynamicResource SourceLogo2}"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel x:Name="ON" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,0,0,0" >
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <Image RenderOptions.BitmapScalingMode="Fant" Height="155" Margin="2,2,5,2"
									   Source="{DynamicResource SourceLogo}"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Style.Triggers>

                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                        </StackPanel>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Background" Value="{DynamicResource TopBarBackgroundBrush}" TargetName="MainBackground" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BorderFocus" />
                            <Setter Property="Opacity" Value="1" TargetName="OFF" />
                            <Setter Property="Opacity" Value="1" TargetName="ON" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Background" Value="{DynamicResource TopBarBackgroundBrush}" TargetName="MainBackground" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BorderFocus" />
                            <Setter Property="Opacity" Value="1" TargetName="OFF" />
                            <Setter Property="Opacity" Value="1" TargetName="ON" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Visibility" TargetName="ON" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Visibility" TargetName="OFF" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MoreGameButton" TargetType="ToggleButton">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Foreground" Value="Transparent" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="60" />
        <Setter Property="Width" Value="60" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid x:Name="MainGrid" Background="Transparent" >
                        <Border CornerRadius="5" Opacity="0.4" Background="Transparent" x:Name="MainBackground"/>
                        <Border x:Name="MainBorder" Opacity="0" Margin="-6"
										SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
											Tag="False"
											BorderThickness="3" CornerRadius="5" Visibility="Collapsed" />
                        <Border x:Name="BorderFocus" Margin="-3"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                BorderBrush="{DynamicResource ButtonBrushSetting}" CornerRadius="5"
                                BorderThickness="3" Visibility="Collapsed"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <StackPanel x:Name="OFF" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,0,0,0" >
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,15">
                                    <Image RenderOptions.BitmapScalingMode="Fant" Height="120" Margin="2,15,2,2"
									   Source="{DynamicResource VerticalLogo}"  HorizontalAlignment="Center" VerticalAlignment="Center" />

                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,15">
                                    <Image RenderOptions.BitmapScalingMode="Fant" Height="120" Margin="2,15,2,2"
									   Source="{DynamicResource VerticalLogo}"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,15">
                                    <Image RenderOptions.BitmapScalingMode="Fant" Height="120" Margin="2,15,2,2"
									   Source="{DynamicResource VerticalLogo}"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel x:Name="ON" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2,0,0,0" >
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <Image RenderOptions.BitmapScalingMode="Fant" Height="120" Margin="2,15,2,2"
									   Source="{DynamicResource HorizontalLogo}"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Style.Triggers>

                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                        </StackPanel>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Background" Value="{DynamicResource TopBarBackgroundBrush}" TargetName="MainBackground" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BorderFocus" />
                            <Setter Property="Opacity" Value="1" TargetName="OFF" />
                            <Setter Property="Opacity" Value="1" TargetName="ON" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="1" TargetName="MainBackground" />
                            <Setter Property="Background" Value="{DynamicResource TopBarBackgroundBrush}" TargetName="MainBackground" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BorderFocus" />
                            <Setter Property="Opacity" Value="1" TargetName="OFF" />
                            <Setter Property="Opacity" Value="1" TargetName="ON" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrushDark}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Visibility" TargetName="ON" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Visibility" TargetName="OFF" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TrailerButtonDetails" TargetType="ToggleButton">
        <Setter Property="IsChecked" Value="{Binding ElementName=ExtraMetadataLoader_VideoLoaderControl_NoControls_Sound, Path=Content.SettingsModel.Settings.IsVideoPlaying, Mode=OneWay, FallbackValue={StaticResource False}}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="IsChecked" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <ControlTemplate.Resources>
                        <Storyboard x:Key="BorderAnim">
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" Storyboard.TargetName="BorderFocus" From="0" To="1" Duration="0:0:0.15" FillBehavior="Stop"/>
                        </Storyboard>
                        <Storyboard x:Key="PlayAnimOn">
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" Storyboard.TargetName="PlayButton" From="1" To="0" Duration="0:0:0.2" FillBehavior="HoldEnd"/>
                        </Storyboard>
                        <Storyboard x:Key="PlayAnimOff">
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" Storyboard.TargetName="PlayButton" From="0" To="1" Duration="0:0:0.2" FillBehavior="HoldEnd"/>
                        </Storyboard>
                    </ControlTemplate.Resources>
                    <Border BorderBrush="Transparent" CornerRadius="8">
                        <Grid >
                            <Border Name="mask" Background="{TemplateBinding Background}" CornerRadius="8"/>
                            <Grid>
                                <Grid.OpacityMask>
                                    <VisualBrush Visual="{Binding ElementName=mask}"/>
                                </Grid.OpacityMask>
                            </Grid>
                            <Image x:Name="PlayButton" Opacity="1" RenderOptions.BitmapScalingMode="Fant" Height="95" Source="{ThemeFile 'Images/PlayTrailer.png' }" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Image.Effect>
                                    <DropShadowEffect BlurRadius="50" ShadowDepth="0"/>
                                </Image.Effect>
                            </Image>

                            <Border x:Name="BorderFocus" Margin="0" CornerRadius="5"
                                BorderBrush="{DynamicResource ButtonBrushSetting}"
                                BorderThickness="3" Visibility="Hidden">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="0" Opacity="1" 
                                                  Color="Black"                                                  
                                                  RenderingBias="Performance" BlurRadius="5"/>
                                </Border.Effect>
                            </Border>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource PlayAnimOff}"/>
                            </Trigger.ExitActions>
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource PlayAnimOn}"/>
                            </Trigger.EnterActions>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="BorderFocus" />
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource BorderAnim}"/>
                            </Trigger.EnterActions>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Visibility" Value="Visible" TargetName="BorderFocus" />
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource BorderAnim}"/>
                            </Trigger.EnterActions>
                        </Trigger>
                        <DataTrigger Binding="{Binding ElementName=FocusLogic, Path=MinHeight}" Value="1">
                            <Setter Property="Focusable" Value="True"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=PART_ElemGameDetails, Path=Visibility}" Value="Collapsed">
                            <Setter Property="IsChecked" Value="False" />
                        </DataTrigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            
        </Style.Triggers>
    </Style>

    <Style TargetType="ToggleButtonEx" BasedOn="{StaticResource {x:Type ToggleButton}}">
    </Style>
</ResourceDictionary>